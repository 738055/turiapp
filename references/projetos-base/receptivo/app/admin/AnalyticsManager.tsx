'use client';

// Este componente foi desativado para evitar conflitos com o novo sistema de Analytics global.
// Mantemos o arquivo apenas para não quebrar importações existentes no painel admin.

export default function AnalyticsManager(props: any) {
  return null;
}